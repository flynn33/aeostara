
# Execution and Verification

Execution consumes `ExecutionContext`, `RecoveryStep`, and `VerificationPlan`. Verification plans are generated before mutation; execution results and verification results are linked to diagnostic and audit chains.
