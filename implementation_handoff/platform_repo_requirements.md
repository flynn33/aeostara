# Platform Repo Requirements

Platform repos must pin Aeostara, map contracts/interfaces, run fixtures, emit diagnostics/audit, and record deviations without changing base semantics.

Final judgments must be `CONFORMANT`, `CONFORMANT WITH CAVEATS`, or `NON-CONFORMANT`.
