# Required Contracts

Required contracts are every schema under `specs/contracts/`, including ASH, JSON, projection, diagnostics, recovery, policy, backup, execution, verification, rollback, audit, heal, and dry-run results.

Final judgments must be `CONFORMANT`, `CONFORMANT WITH CAVEATS`, or `NON-CONFORMANT`.
