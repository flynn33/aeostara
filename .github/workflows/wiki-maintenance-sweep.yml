name: Wiki Maintenance Sweep

on:
  workflow_dispatch:

jobs:
  sweep:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Clone wiki repository
        run: |
          set -euo pipefail
          git clone "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git" wiki || {
            mkdir -p wiki
            cd wiki
            git init
            git checkout -b main
            git remote add origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git"
          }

      - name: Generate pages for base design
        run: |
          set -euo pipefail

          branches=("main")

          for branch in "${branches[@]}"; do
            slug="${branch//\//__slash__}"
            wt="worktree-${slug}"

            git worktree add "$wt" "origin/$branch"
            commit_sha="$(git -C "$wt" rev-parse HEAD)"

            python3 "$wt/.github/scripts/generate_wiki_pages.py" \
              --src-dir "$wt" \
              --wiki-dir "wiki" \
              --branch-name "$branch" \
              --commit-sha "$commit_sha"

            git worktree remove "$wt" --force
          done

      - name: Commit and push wiki updates
        run: |
          set -euo pipefail
          cd wiki

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A
          git diff --cached --quiet && echo "No wiki changes" && exit 0

          git commit -m "docs(wiki): maintenance sweep"

          WIKI_BRANCH="main"
          git ls-remote --heads origin master >/dev/null 2>&1 && WIKI_BRANCH="master"

          if ! git fetch origin "$WIKI_BRANCH" 2>/dev/null; then
            echo "No existing wiki branch fetched; continuing with generated pages"
          fi
          if ! git rebase "origin/${WIKI_BRANCH}" 2>/dev/null; then
            if git rebase --abort 2>/dev/null; then
              echo "Rebase aborted after wiki branch conflict"
            fi
          fi

          git push origin "HEAD:${WIKI_BRANCH}"
